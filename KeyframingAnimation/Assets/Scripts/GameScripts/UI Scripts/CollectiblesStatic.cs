using UnityEngine;
using System.Collections;

public class CollectiblesStatic : MonoBehaviour {

    public static bool[] collectibles = {false, false, false};

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
